<% content_for :title do %>
    <%= t('.menus') %>
<% end %>

<% content_for :page_title do %>
    <i class="fa fa-th-list"></i> <%= t('.menus') %>
<% end %>

<%= erp_datalist(
    url: erp_hkstore.list_backend_menus_path,
    keywords: [
        {
            text: t('.name'), name: 'erp_hkstore_menus.name'
        }
    ],
    actions: [
    ],
    filters: [
    ],
    columns: [
        {
            text: t('.name'), name: 'name', value: true, checked: true
        },
        {
            text: t('created_at'), name: 'created_at', value: true, checked: false
        }
    ],
    sort: { sort_by: 'erp_hkstore_menus.created_at', sort_direction: 'desc' }
) %>